# مشخصات زبان برنامه‌نویسی کلنگ

زبانی فارسی‌محور با حس طبیعیِ متن فارسی، هم‌تراز با پایتون در قابلیت‌ها و الهام‌گرفته از Go در استواری

## ۱. فلسفه و اهداف

**کلنگ** (Kolang) زبانی اسکریپتی است که مانند پایتون عمل می‌کند، ولی کد منبع آن فارسی است. هدف اصلی این است که **خواندن کد، مانند خواندن یک دستور فارسی طبیعی** باشد. به همین دلیل:

- **فارسیِ محض در کد منبع.** کلمات کلیدی و افعال همیشه فارسی هستند؛ شناسه‌ها می‌توانند فارسی یا انگلیسی باشند (حروف، اعداد، `_`، نیم‌فاصله). فقط در بلوک `پایتون:` کد لاتین کامل مجاز است.
- **ترتیب طبیعیِ جمله فارسی.** فعل در پایان قرار می‌گیرد. به‌جای `print("سلام")` می‌نویسیم `«سلام» بنویس` — «سلام بنویس».
- **اضافه‌ی فارسی به‌جای نقطه.** دسترسی به اعضای ماژول و صفات شیء با اضافه‌ی kasra (`ِ`) انجام می‌شود، نه با نقطه: `جذرِ ریاضی` نه `ریاضی.جذر`، `عنوانِ خود` نه `خود.عنوان`. این کلنگ را شبیه نثر فارسی می‌کند.
- **جداکننده‌ی آرگومان‌ها «و» است.** به‌جای ویرگول: `جمع(۱ و ۲ و ۳)`، `تعریف جمع(الف و ب):`. «و» در بافت پرانتز (و در انتساب چندتایی سمت چپ) به‌عنوان جداکننده رفتار می‌کند؛ عملگر منطقی «and» کلمه‌ی `همچنین` است (بیرون پرانتز).
- **عملگرهای نمادین.** ضرب با `×`، تقسیم با `÷` (نماد فارسی‌پسند)، توان با `*`، تقسیم صحیح با `÷/`. انتساب و جمع/منها نمادین می‌مانند.
- **بدون درستی ضمنی.** شرط باید همیشه مقایسه‌ی صریح باشد؛ `اگر x:` نامعتبر است.
- **هم‌تراز با پایتون.** تمام قابلیت‌های پایتون (متغیر، تابع، گونه، وارث، استثنا، جنریتور، پوشش، ماژول، ناهمگام) پشتیبانی می‌شود.
- **الهام از Go برای استواری.** مقدارهای برگشتی چندتایی، نوع صریح خطا، تارک‌ها، کانال‌ها، تأخیر و رابط‌ها. Go
- **دسترسی به کتابخانه‌ی پایتون.** ماژول‌ها و توابع با نام فارسی در دسترس‌اند و در صورت نیاز می‌توان کد پایتون را مستقیماً جاسازی کرد.
- **معماری.** لِکسر ← پارسر ← AST ← ارزیاب درختی (tree-walking). کامپایلر، بایت‌کد و ماشین مجازی در نسخه‌های بعدی (v0.7+) برنامه‌ریزی شده‌اند.

## ۲. ساختار کلی

یک برنامه‌ی کلنگ مجموعه‌ای از **دستورها** است که با خطوط جدا می‌شوند. مانند پایتون، **تورفتگی** (۴ فاصله) ساختار بلوک‌ها را تعیین می‌کند. پایان دستور با **خط جدید** است.

### ۲.۱ سلام دنیا

```kolang
«سلام دنیا!» بنویس
```

### ۲.۲ تعریف تابع و فراخوانی

```kolang
تعریف جمع(الف و ب):
    الف + ب برگردان

جمع(۳ و ۵) بنویس
```

### ۲.۳ شرط

```kolang
اگر سن >= ۱۸ باشد:
    «بزرگسال» بنویس
وگرنه:
    «کودک» بنویس
```

## ۳. لغت‌شناسی (Lexical)

### ۳.۱ نویسه‌ها

کد منبع باید با یونیکد (UTF-8) ذخیره شود.

#### ۳.۱.۱ ارقام

- ارقام فارسی `۰ ۱ ۲ ۳ ۴ ۵ ۶ ۷ ۸ ۹` پذیرفته و در لِکسر به لاتین نرمال می‌شوند.
- صحیح: `۱۲۳`، اعشاری: `۱۲٫۵` (نماد `٫` U+066B یا `.`).
- جداکننده‌ی گروه ارقام: `۱٬۲۳۴` (نماد `٬` U+066C) یا `۱,۲۳۴,۵۶۷` (ویرگول ASCII) معتبر است و در لِکسر حذف می‌شود.
- پیشوندها: `۰x` مبنای ۱۶، `۰b` باینری، `۰o` اکتال.

#### ۳.۱.۲ شناسه‌ها

شناسه با حرف فارسی آغاز می‌شود و می‌تواند شامل حرف فارسی، رقم، خط زیر `_` و **نیم‌فاصله** (U+200C) باشد. نیم‌فاصله بخشی از شناسه است (نه جداکننده).

```kolang
نام
سنِکاربر
_متغیر
تابع۱
امانت‌دادن
```

#### ۳.۱.۳ اضافه‌ی kasra (U+0650) — عملگر دسترسی به اعضا

کلنگ از اضافه‌ی فارسی (`ِ` U+0650) به‌عنوان **تنها عملگر دسترسی به اعضا** استفاده می‌کند. نقطه (`.`) برای این منظور به‌کار نمی‌رود. اضافه بین دو شناسه قرار می‌گیرد و به‌معنای «ِ» (kasra) در فارسی است:

```kolang
جذرِریاضی          /معادل math.sqrt — صفت
عنوانِخود          /معادل self.title — صفت
صدادهیِ()خود       /معادل self.speak() — فراخوانی متد
روشِ()والدِخود  /معادل self.parent.method() — زنجیره
```

##### قواعد گرامری اضافه

اضافه در کلنگ تابع قواعد زیر است:

1. **اضافه همیشه یک توکن جداگانه است.** چه فاصله‌ای گرد آن باشد چه نباشد، `الفِ ب` و `الفِب` در لِکسر یکسان تایم‌بندی می‌شوند: `الف` + `ِ` + `ب`. اضافه هرگز بخشی از شناسه نیست.
1. **اضافه فقط به حرف می‌چسبد.** در فارسی، kasra یک نشانه‌ی ترکیبی (combining mark) است که زیر یک حرف قرار می‌گیرد. پس اضافه باید بعد از یک حرف (شناسه) بیاید، نه بعد از علائم نگارشی. `صدادهیِ` درست است (kasra زیر «ی»); `()ِ` نامعتبر است.
1. **زنجیره‌ی اضافه از راست به چپ است.** `روشِ والدِ خود` = `روشِ (والدِ خود)` = روشِ (والدِ (خود)). این همان ترتیب طبیعی possession در فارسی است: چیزِ متعلّق‌به در ابتدا، مالک در انتها.
1. **دسترسی به صفت:** `عنوانِ خود` — اضافه بین نام صفت و گیرنده. بدون پرانتز. معادل `self.title`.
1. **فراخوانی متد:** `صدادهیِ()خود` — اضافه پس از نام متد (زیر حرف آخر نام)، سپس `()` یا `(آرگ‌ها)`، سپس گیرنده. این شکل می‌گوید: «صدادهیِ متعلّق‌به خود، سپس فراخوانی». پرانتزها به‌روشنی به نام متد چسبیده‌اند چون بلافاصله پس از اضافه می‌آیند. معادل `self.speak()`.
  - با آرگومان: `جمعِ(الف و ب)والدِ خود` = `self.parent.add(a, b)`
  - زنجیره: `روشِ()والدِ خود` = `self.parent.method()`
1. **سمت چپ انتساب.** `نامِ خود = مقدار` به `self.name = value` ترجمه می‌شود. زنجیره‌ی اضافه در سمت چپ `=` به‌عنوان هدفِ ذخیره‌سازی تفسیر می‌شود (store-target).

در لِکسر، اضافه به‌عنوان یک **عملگر میان‌گذاری** شناخته می‌شود (مانند `+` یا `-`)، نه بخشی از شناسه. در پارسر، الگوی فراخوانی متد به‌صورت `NAMEِ(args)receiver` تایم‌بندی می‌شود: `()` به نام متد چسبیده و اضافه، نام متد را به گیرنده متصل می‌کند.

> نکته: نقطه (`.`) در کلنگ فقط برای اعداد اعشاری استفاده می‌شود (`۳٫۱۴`) و دیگر عملگر دسترسی نیست. در v2 (VM مستقل)، برای مسیر ماژول (`::`) و generic (`<ت>`) از نمادهای جداگانه استفاده خواهد شد.

#### ۳.۱.۴ رشته‌ها

رشته با **گیومه‌های فرانسوی** `« »` تعریف می‌شود. رشته‌ها چندخطی‌اند و **متن‌های «» از نوع f-string هستند**: عبارات داخل `{...}` ارزیابی می‌شوند. برای نمایش `{...}` لفظی از `{{` و `}}` استفاده کنید. رشته‌ها از نوع `متن` هستند:

```kolang
«سلام»
«این یک رشته‌ی چندبخشی است»
```

چندخطی به‌طور طبیعی پشتیبانی می‌شود:

```kolang
«این
یک
رشته‌ی
چندخطی است»
```

**درون‌یابی** (interpolation) با آکولاد (f-string-like):

```kolang
نام = «علی»
«سلام {نام}!» بنویس     /چاپ: سلام علی!
```

برای نمایش literal آکولاد، آن را دو بار بنویسید: `{{` و `}}`.

#### ۳.۱.۵ توضیحات (Comments)

توضیحات با **خط اریب** نوشته می‌شوند (نه با `#`):

- **خطی:** `/این یک توضیح است`
- **چندخطی:** `//این یک توضیح چندخطی است//`

```kolang
/توضیح خطی
//توضیح
چندخطی//
```

### ۳.۲ عملگرها

| عملگر | معنی | مثال |
|---|---|---|
| `=` | قرار دادن (انتساب) | `الف = ۵` |
| `==` | برابری — تنها شکل مقایسه در عبارات و شرط‌ها (نفی با `نباشد`) | `الف == ب` |
| `<` `>` `<=` `>=` | مقایسه — تنها شکل مقایسه در عبارات و شرط‌ها | `الف < ب` |
| `+` `-` | جمع/منها/الحاق | `الف + ب` |
| `×` | ضرب | `الف × ب` |
| `÷` | تقسیم | `الف ÷ ب` |
| `÷/` | تقسیم صحیح | `الف ÷/ ب` |
| `%` | باقی‌مانده | `الف % ب` |
| `*` | توان | `الف * ۲` |
| `+=` `-=` `×=` `÷=` `*=` `÷/=` `%=` | انتساب مرکب | `الف += ۱` |
| `همچنین` `یا` `نباشد` | و / یا / نفی منطقی | `الف همچنین ب`، `ب نباشد` |
| `ِ` (اضافه) | دسترسی به عضو / صفت / متد | `جذرِ ریاضی`، `عنوانِ خود` |
| `->` | پیکان (نوع خروجی تابع یا لامبدا) | `تعریف ف() -> صحیح:` |
| `\|>` | لوله (زنجیره‌ی فراخوانی) | `الف \|> ف \|> گ` |
| `<<` `>>` | ارسال (`<<`) / دریافت (`>>`) کانال Go | `ch << مقدار`، `مقدار = >>ch` |
| `و` (درون پرانتز) | جداکننده‌ی آرگومان | `جمع(۱ و ۲ و ۳)` |

> نکته: «و» در بافت پرانتز (و در انتساب چندتایی سمت چپ) جداکننده‌ی آرگومان است. عملگر منطقی «and» کلمه‌ی `همچنین` است (بیرون پرانتز). نفی منطقی `نباشد` است.

### ۳.۳ کلمات کلیدی

| کلمه کلیدی | معادل پایتون / Go | توضیح |
|---|---|---|
| `تعریف` | def | تعریف تابع |
| `برگردان` | return | فعل: `x برگردان` |
| `اگر` | if | شرط |
| `وگرنه` | else | در غیر این صورت |
| `وگرنه اگر` | elif | در غیر این صورت اگر |
| `برای` | for | حلقه |
| `تاوقتی` | while | تا وقتی که |
| `در` | in | عضویت/پیمایش |
| `از ... تا ... گام ...` | range | دامنه‌ی حلقه |
| `اتمام` | break | خروج از حلقه |
| `بروبعدی` | continue | پرش به تکرار بعدی |
| `گونه` | class / struct | تعریف گونه (نوع داده‌ی کاربر) |
| `رابط` | interface Go | تعریف رابط (ساختاری) |
| `رهی` | implements | اعلان صریح پیاده‌سازی رابط (اختیاری) |
| `وارث` | extends | نشانگر ارث‌بری در گونه |
| `خود` | self | ارجاع به نمونه |
| `ساخت` | __init__ | سازنده‌ی گونه |
| `والد` | super | ارجاع به گونه‌ی والد |
| `درست` | True | بولی درست |
| `غلط` | False | بولی غلط |
| `تهی` | None | هیچ |
| `بیار` | import | فعل پایانی: `ریاضی بیار` (فقط import) |
| `از` | from | برای `از ریاضی جذر بیار` |
| `بانام` | as | نام‌گذاری هنگام گرفتن |
| `بپا` | try | بلوک امتحان (فعل: «برپا کن/کوشش کن») |
| `درنهایت` | finally | بلوک نهایی |
| `بده` | raise | فعل: `x بده` |
| `با` | with | مدیر محتوا |
| `جهانی` | global | متغیر جهانی |
| `نامحلی` | nonlocal | متغیر غیرمحلی |
| `تابع` | lambda | تابع بدون نام |
| `مثل` | pass | دستور خالی |
| `حذف` | del | حذف |
| `ادعا` | assert | ادعا |
| `منتظر` | await | در توابع ناهمگام |
| `ناهمگام` | async | تعریف تابع ناهمگام |
| `بساز` | yield | فعل: `ای بساز` (تولید مقدار در جنریتور) |
| `بساز‌از` | yield from | تولید از |
| `پوشش` | @ (decorator) | پوشش تابع با لایه‌ای دیگر — `پوشش نام` یا `پوشش نام(آرگ‌ها)` (خط بالای تعریف) |
| `بنویس` | print (Python 3) | فعل: `x بنویس` — بدون پرانتز |
| `بگیر` | input / except | فعل: `نام بگیر` (ورودی) یا `خطای‌صفر بگیر:` (استثنا) |
| `بیافزا` | append | فعل: `y به x بیافزا` |
| `حذف‌کن` | remove | فعل: `y از x حذف‌کن` |
| `ببند` | close Go | فعل: `ch ببند` (بستن کانال) |
| `باشد` | — | فعل التزامی مثبت: `اگر x == ۵ باشد:`؛ آزمون عضویت: `اگر x در فهرست باشد:` |
| `نباشد` | — | فعل التزامی منفی (نفی در شرط‌ها): `اگر x == تهی نباشد:`؛ نفی منطقی: `x نباشد` = not x؛ نفی مقایسه: `اگر x == ۵ نباشد:` (در پایتون: x != ۵) |
| `است` | — | فعل ربطی (برای ادعا/حالت) |
| `برو` | go Go | اجرای تارک: `برو کار()` |
| `تأخیری` | defer postfix Go | `پاک‌سازی() تأخیری` |
| `کانال` | chan Go | ساختن کانال: `کانال(صحیح و ۱۰)` |
| `خطا` | error Go | نوع صریح خطا |

## ۴. انواع داده‌ای

| نوع در کلنگ | معادل پایتون / Go | مثال |
|---|---|---|
| `صحیح` | int | `۱۲۳` |
| `اعشاری` | float | `۱۲٫۵` |
| `مختلط` | complex | `۳ + ۲ی` |
| `بولی` | bool | `درست`، `غلط` |
| `متن` | str | `«متن»` |
| `بایت` | bytes | `ب(«abc»)` |
| `فهرست` | list | `[۱ و ۲ و ۳]` |
| `قفسه` | tuple | `(۱ و ۲ و ۳)` |
| `مجموعه` | set | `{۱ و ۲ و ۳}` |
| `مجموعه‌ثابت` | frozenset | `ثابت({۱ و ۲})` |
| `گنجه` | dict | `{«الف»: ۱ و «ب»: ۲}` |
| `تهی‌ها` | NoneType | `تهی` |
| `خطا` | error Go | `خطا(«پیام»)` |
| `کانال` | chan Go | `کانال(صحیح و ۱۰)` |
| `تارک` | goroutine Go | نوع برگشتی `برو` |

### ۴.۱ لیترال‌ها

```kolang
/عدد صحیح
۱۲۳
/اعشاری
۱۲٫۵
/بولی
درست
غلط
/رشته (گیومه)
«سلام»
/فهرست (با «و»)
[۱ و ۲ و ۳]
/قفسه
(۱ و ۲ و ۳)
/گنجه
{«نام»: «علی» و «سن»: ۲۰}
/مجموعه
{۱ و ۲ و ۳}
/خطا
خطا(«مشکلی پیش آمد»)
```

### ۴.۲ سیستم نوع ( gradual typing )

کلنگ از **نوع‌دهی تدریجی** (gradual typing) پیروی می‌کند: حاشیه‌نویسی‌های نوع **اختیاری** هستند و در زمان اجرا بررسی می‌شوند. این یعنی می‌توان بدون هیچ حاشیه‌نویسی نوشت (مثل پایتون خالص) یا برای خوانایی و ایمنی بیشتر، نوع‌ها را صریح اعلام کرد. در نسخه‌های بعدی (v2 با VM مستقل)، این حاشیه‌نویسی‌ها به بررسی زمان-کامپایل تبدیل می‌شوند.

#### ۴.۲.۱ حاشیه‌نویسی متغیر

```kolang
سن: صحیح = ۲۵
نام: متن = «علی»
نرخ: اعشاری = ۳٫۱۴
فعال: بولی = درست
```

اگر مقدار با نوع اعلام‌شده سازگار نباشد، در زمان اجرا خطا داده می‌شود:

```kolang
سن: صحیح = «علی»  /خطای‌نوع در زمان اجرا
```

#### ۴.۲.۲ حاشیه‌نویسی پارامتر و خروجی تابع

```kolang
تعریف جمع(الف: صحیح و ب: صحیح) -> صحیح:
    الف + ب برگردان
```

بدون حاشیه‌نویسی هم معتبر است (نوع‌دهی پویا):

```kolang
تعریف جمع(الف و ب):
    الف + ب برگردان
```

#### ۴.۲.۳ نوع‌های مرکب و خطا

```kolang
/خروجی چندتایی با نوع
تعریف تقسیم‌امن(الف: اعشاری و ب: اعشاری) -> (اعشاری و خطا):
    اگر ب == ۰ باشد:
        تهی و خطا(«تقسیم بر صفر») برگردان
    الف ÷ ب و تهی برگردان
```

> نکته: در v1 حاشیه‌نویسی‌ها اختیاری و در زمان اجرا بررسی می‌شوند (مانند `typing` پایتون با `@runtime_checkable`). در v2 (VM مستقل)، این حاشیه‌نویسی‌ها به بررسی زمان-کامپایل تبدیل می‌شوند و پایه‌ی سیستم نوع استاتیک کلنگ (شامل genericها، traitها، و نوع‌های جمع) خواهند بود.

## ۵. دستورات (Statements)

### ۵.۱ انتساب

```kolang
الف = ۵
الف و ب و ج = ۱ و ۲ و ۳
الف += ۱
الف ×= ۲
```

### ۵.۲ انتساب چندتایی Go

```kolang
تعریف تقسیم‌امن(الف و ب):
    اگر ب == ۰ باشد:
        تهی و خطا(«تقسیم بر صفر») برگردان
    الف ÷ ب و تهی برگردان

نتیجه و خط = تقسیم‌امن(۱۰ و ۲)
اگر خط == تهی نباشد:
    پیامِخط بنویس
وگرنه:
    نتیجه بنویس
```

### ۵.۳ شرط

در کلنگ **هیچ درستی ضمنی (implicit truthiness) وجود ندارد**: یک متغیر یا عبارت لخت نمی‌تواند شرط باشد. شرط باید همیشه یک **مقایسه‌ی نمادین صریح** باشد: `اگر x == ۵ باشد:`، `اگر x >= ۰ نباشد:`. `اگر فهرست:` یا `اگر x:` (به‌سبک پایتون) **نامعتبر** است.

تنها استثنا مقادیر واقعاً بولی هستند که با `== درست` / `== غلط` مقایسه می‌شوند — حتی `درست` به‌تنهایی به‌عنوان شرطِ حلقه‌ی بی‌پایان (`تاوقتی درست باشد:`) پذیرفته است چون خودش یک مقدار بولی صریح است.

```kolang
اگر نمره >= ۱۷ باشد:
    «عالی» بنویس
وگرنه اگر نمره >= ۱۴ باشد:
    «خوب» بنویس
وگرنه:
    «قابل قبول» بنویس
```

بررسی خالی بودن:

```kolang
اگر طول(فهرست) == ۰ باشد:
    «فهرست خالی است» بنویس
```

بررسی عضویت:

```kolang
اگر آیتم در فهرست باشد:
    «وجود دارد» بنویس
```

بررسی خطا Go:

```kolang
اگر خط == تهی نباشد:
    پیامِخط بنویس
    اتمام
```

بررسی یک پرچم بولی:

```kolang
اگر پرچم == درست باشد:
    «فعال» بنویس
```

> این قانون از خطاهای خاموش پایتون (`if x:` وقتی `x = 0` یا `x = []`) جلوگیری می‌کند و صراحت Go را به‌جا می‌گذارد.

### ۵.۴ حلقه‌ی `تاوقتی` (while)

```kolang
تاوقتی شمارنده < ۱۰ باشد:
    شمارنده بنویس
    شمارنده += ۱
```

### ۵.۵ حلقه‌ی `برای` (for)

```kolang
برای آیتم در فهرستِکارها:
    آیتم بنویس

برای ای از ۰ تا ۱۰:
    ای بنویس

برای ای از ۰ تا ۱۰ گام ۲:
    ای بنویس

برای ای از ۱۰ تا ۰ گام -۱:
    ای بنویس
```

شمارش با اندیس:

```kolang
برای ای و مقدار در شمارش(فهرست):
    ای و «: » و مقدار بنویس
```

### ۵.۶ `اتمام` و `بروبعدی`

```kolang
برای ای در بازه(۱۰):
    اگر ای == ۵ باشد:
        اتمام
    اگر ای % ۲ == ۰ باشد:
        بروبعدی
    ای بنویس
```

### ۵.۷ تعریف تابع

```kolang
تعریف ضرب(الف و ب):
    الف × ب برگردان

/مقدار پیش‌فرض
تعریف سلام(نام = «دنیا»):
    «سلام » + نام بنویس

/آرگومان کلیدی-واژه‌ای
تعریف اطلاعات(**اطلاعات):
    برای کل و مقدار در مواردِاطلاعات:
        کل و «: » و مقدار بنویس

/آرگومان متغیر
تعریف جمع‌همه(*اعداد):
    نتیجه = ۰
    برای ن در اعداد:
        نتیجه += ن
    نتیجه برگردان
```

#### تابع بدون نام (لامبدا)

```kolang
دوبرابر = تابع(الف) -> الف × ۲
```

#### توابع ناهمگام

```kolang
ناهمگام تعریف دریافت‌داده():
    داده = منتظر درخواست()
    داده برگردان
```

### ۵.۸ تعریف گونه (class)

```kolang
گونه حیوان:
    ساخت(خود و نام):
        نامِخود = نام

    تعریف صدادهی(خود):
        «صدای حیوان» بنویس

گونه سگ وارث حیوان:
    تعریف صدادهی(خود):
        «واف واف» بنویس

    تعریف معرفی(خود):
        صدادهیِ()والدِخود
        «من یک سگ هستم به نام » + نامِخود بنویس
```

> توجه: `نامِ خود = نام` به `self.name = name` ترجمه می‌شود. اضافه (`ِ`) در سمت چپ انتساب هم عملگر دسترسی است.

### ۵.۹ رابط‌ها Go

رابط مجموعه‌ای از امضاهای متد است. گونه‌ها به‌صورت **ساختاری** (implicit) رابط را پیاده می‌کنند:

```kolang
رابط حیوان:
    تعریف صدادهی(خود)
    تعریف نام(خود) -> متن

گونه گربه:
    ساخت(خود و نام):
        نامِخود = نام

    تعریف صدادهی(خود):
        «میو» بنویس

    تعریف نام(خود) -> متن:
        نامِخود برگردان

/گربه خودبه‌خود رابط حیوان را پیاده‌سازی می‌کند.
تعریف معرفی(ح: حیوان):
    صدادهیِ()ح
    نامِ()ح بنویس

معرفی(گربه(«پشمک»))
```

اعلان صریح با `رهی` اختیاری است:

```kolang
گونه گربه رهی حیوان:
    ...
```

### ۵.۱۰ گرفتن ماژول‌ها (import)

دستور import با فعل طبیعی **«بیار»** در پایان جمله نوشته می‌شود (بدون «را»):

```kolang
/گرفتن کل ماژول
ریاضی بیار

/گرفتن یک نام از ماژول
از ریاضی جذر بیار

/گرفتن با نام مستعار
از ریاضی جذر بانام ریشه بیار

/استفاده (با اضافه)
جذرِریاضی(۱۶) بنویس
ریشه(۲۵) بنویس
```

### ۵.۱۱ استثناها

شکل طبیعی با افعال «بپا»، «بگیر» (فعل‌پایانی)، «بده»:

```kolang
بپا:
    نتیجه = ۱۰ ÷ ۰
خطای‌صفر بگیر:
    «تقسیم بر صفر!» بنویس
درنهایت:
    «پاک‌سازی» بنویس

/پرتاب استثنا
اگر سن < ۰ باشد:
    خطای‌مقدار(«سن نمی‌تواند منفی باشد») بده
```

گرفتن با نام متغیر:

```kolang
بپا:
    نتیجه = ۱۰ ÷ ۰
خطای‌صفر بگیر بانام err:
    پیامِerr بنویس
```

### ۵.۱۲ مدیر محتما (`با`)

```kolang
با بازکردن(«فایل.txt» و «r») بانام ف:
    محتوا = بخوانِ()ف
```

### ۵.۱۳ تأخیر Go

```kolang
تعریف پردازش(مسیر):
    ف = بازکردن(مسیر و «r»)
    ببندِ()ف تأخیری
    /... کار با ف ...
    /ببندِ ف() در پایان تابع به‌طور خودکار اجرا می‌شود
```

### ۵.۱۴ جنریتورها

```kolang
تعریف اعداد‌زوج(حد):
    برای ای از ۰ تا حد:
        اگر ای % ۲ == ۰ باشد:
            ای بساز

برای ن در اعداد‌زوج(۱۰):
    ن بنویس
```

### ۵.۱۵ پوشش‌ها (decorators)

پوشش، تابع را با لایه‌ای از رفتار اضافی می‌پیچد. کلمه‌ی `پوشش` به‌معنای «در بر گرفتن/پوشاندن» است: تابع اصلی درون پوشش قرار می‌گیرد.

```kolang
پوشش زمان‌سنج
تعریف تابع‌کند():
    «در حال اجرا...» بنویس
```

چند پوشش (پایینی‌ترین نخست اعمال می‌شود):

```kolang
پوشش الف
پوشش ب
تعریف ف():
    مثل
```

پوشش با آرگومان:

```kolang
پوشش مسیر(«/api»)
تعریف کاربر():
    «مدیریت کاربر» بنویس
```

## ۶. عبارات (Expressions)

### ۶.۱ عملگرهای حسابی

```kolang
۱ + ۲
۳ × ۴
۱۰ ÷ ۳
۱۰ ÷/ ۳
۱۰ % ۳
۲ * ۱۰
```

### ۶.۲ عملگرهای منطقی

```kolang
درست همچنین غلط
درست یا غلط
درست نباشد
```

### ۶.۳ عملگرهای مقایسه‌ای (در عبارات خالص)

```kolang
الف == ب
الف < ب
الف >= ب
```

### ۶.۴ فهرست‌سازی و برش

```kolang
فهرست[۰]
فهرست[۱:۳]
فهرست[::۲]
رشته[۰]
گنجه[«کلید»]
```

### ۶.۵ فراخوانی تابع و متد

```kolang
/فراخوانی تابع (آرگومان‌ها با «و»)
طول(فهرست)
جمع(۱ و ۲ و ۳)
تابع(الف=۱ و ب=۲)

/دسترسی به صفت (اضافه، بدون پرانتز)
عنوانِخود
جذرِریاضی

/فراخوانی متد (اضافه + پرانتز + گیرنده)
صدادهیِ()خود
جمع‌کنِ(۱۰)والدِخود
```

> الگوی فراخوانی متد: `methodNameِ(args)receiver` — اضافه پس از نام متد (زیر حرف آخر)، سپس پرانتز با آرگومان‌ها، سپس گیرنده. صفت‌ها (بدون فراخوانی) فقط اضافه دارند: `attrِ receiver`.

### ۶.۶ عملگر لوله (`|>`)

```kolang
نتیجه = ۵
    |> ضرب‌به(۲)
    |> جمع‌کن(۱۰)
    |> بنویس
```

معادل:

```kolang
بنویس(جمع‌کن(ضرب‌به(۵ و ۲) و ۱۰))
```

### ۶.۷ عبارت شرطی (سه‌گانه)

```kolang
پیام = «بزرگسال» اگر سن >= ۱۸ باشد وگرنه «کودک»
```

### ۶.۸ درک فهرست/گنجه/مجموعه (Comprehension)

```kolang
/فهرست
مربع‌ها = [ای * ۲ برای ای در بازه(۱۰) اگر ای % ۲ == ۰ باشد]

/گنجه
زوج‌ها = {ای: ای * ۲ برای ای در بازه(۱۰)}

/مجموعه
منحصر = {ای % ۳ برای ای در بازه(۱۰)}

/جنریتور
زوج‌ها = (ای برای ای در بازه(۱۰) اگر ای % ۲ == ۰ باشد)
```

## ۷. توابع داخلی (Builtins)

افعال امری (مثل `بنویس`، `بگیر`) به‌شکل فعل‌پایانی (بدون «را») استفاده می‌شوند؛ بقیه به‌شکل فراخوانی با پرانتز و آرگومان‌های جدا شده با «و». **چاپ به‌سبک پایتون ۳:** `بنویس` هم به‌شکل فعل‌پایانی (`x بنویس`)، هم به‌عنوان هدف لوله (`x |> بنویس`)، و هم به‌شکل فراخوانی (`بنویس(x)`) استفاده می‌شود.

| کلنگ | پایتون / Go | شکل استفاده |
|---|---|---|
| `بنویس` | print (verb) | `x بنویس` |
| `بگیر` | input / except | `نام بگیر` / `خطای‌صفر بگیر:` |
| `برگردان` | return (verb) | `x برگردان` |
| `بیافزا` | append | `y به x بیافزا` |
| `حذف‌کن` | remove | `y از x حذف‌کن` |
| `بده` | raise | `x بده` |
| `اتمام` | break | `اتمام` |
| `بروبعدی` | continue | `بروبعدی` |
| `بیار` | import | `ریاضی بیار` |
| `ببند` | close Go | `ch ببند` |
| `بسته‌است` | closed check Go | `اگر بسته‌استِ ch == درست باشد:` (به‌صورت صفت/attribute استفاده می‌شود) |
| `برو` | go Go | `برو کار()` |
| `تأخیری` | defer postfix Go | `پاک‌سازی() تأخیری` |
| `طول` | len | `طول(فهرست)` |
| `نوع` | type | `نوع(x)` |
| `بازه` | range | `بازه(۱۰)` یا `۰ تا ۱۰` |
| `صحیح` | int | `صحیح(«۱۲»)` |
| `اعشاری` | float | `اعشاری(«۳٫۱۴»)` |
| `متن` | str | `متن(۱۲۳)` |
| `بولی` | bool | `بولی(x)` |
| `فهرست` | list | `فهرست(x)` |
| `قفسه` | tuple | `قفسه(x)` |
| `مجموعه` | set | `مجموعه(x)` |
| `گنجه` | dict | `گنجه(x)` |
| `مرتب` | sorted | `مرتب(x)` |
| `معکوس` | reversed | `معکوس(x)` |
| `شمارش` | enumerate | `شمارش(x)` |
| `بقچه` | zip | `بقچه(الف و ب)` |
| `نگاشت` | map | `نگاشت(f و x)` |
| `پالایش` | filter | `پالایش(f و x)` |
| `جمع` | sum | `جمع(x)` |
| `کمینه` | min | `کمینه(x)` |
| `بیشینه` | max | `بیشینه(x)` |
| `مطلق` | abs | `مطلق(x)` |
| `گرد` | round | `گرد(x)` |
| `بازکردن` | open | `بازکردن(«f.txt»)` |
| `هویت` | id | `هویت(x)` |
| `کمک` | help | `کمک(x)` |
| `اجرا` | eval | `اجرا(«۱+۲»)` |
| `داخلی‌ها` | dir | `داخلی‌ها(x)` |
| `ویژگی` | getattr | `ویژگی(x و «نام»)` |
| `تنظیم‌ویژگی` | setattr | `تنظیم‌ویژگی(x و «نام» و v)` |
| `دارد` | hasattr | `دارد(x و «نام»)` |
| `کانال` | chan Go | `کانال(صحیح و ۱۰)` |
| `خطا` | error Go | `خطا(«پیام»)` |
| `استثنا` | Exception | `استثنا(«پیام»)` |
| `خطای‌مقدار` | ValueError | — |
| `خطای‌کلید` | KeyError | — |
| `خطای‌نوع` | TypeError | — |
| `خطای‌نمایه` | IndexError | — |
| `خطای‌صفر` | ZeroDivisionError | — |
| `خطای‌فایل` | FileNotFoundError | — |
| `توقف‌تکرار` | StopIteration | — |

## ۸. ماژول‌های کتابخانه استاندارد

| کلنگ | پایتون |
|---|---|
| `ریاضی` | math |
| `تصادفی` | random |
| `زمان` | time |
| `تقویم` | datetime |
| `سیستم` | os |
| `مسیر` | os.path |
| `سیستم‌عامل` | sys |
| `رشته‌ها` | string |
| `عبارت‌منظم` / `رجکس` | re |
| `جیسون` | json |
| `اینترنت` | urllib |
| `درخواست` | requests (در صورت نصب) |
| `ریاضی‌آمار` | statistics |
| `مجموعه‌داده` | collections |
| `تابع‌ابزار` | itertools |
| `عملکرد` | functools |
| `آرایه` | array |
| `پایگاه‌داده` | sqlite3 |
| `ریاضی‌پیشرفته` | numpy (در صورت نصب) |
| `پانداز` | pandas (در صورت نصب) |
| `سندان` | pytest (در صورت نصب) |

استفاده:

```kolang
ریاضی بیار
جذرِریاضی(۱۶) بنویس
پیِریاضی بنویس

از تصادفی انتخاب بیار
تصادفیِ()تصادفی بنویس
```

> در صورت نیاز به ماژولی که در جدول نیست، نگاشت در فایل `modules_map.kolang.json` قابل تعریف است.

## ۹. جاسازی کد پایتون (Python Interop)

کلنگ از سه راه با پایتون تعامل دارد:

### ۹.۱ فراخوانی ماژول پایتون با نام فارسی

همان‌طور که در بخش ۸ توضیح داده شد، ماژول‌های پایتون با نام فارسی گرفته می‌شوند.

### ۹.۲ بلوک کد پایتون خام

اگر کاربر بخواهد کد پایتون را مستقیماً درون کلنگ بنویسد، می‌تواند از بلوک `پایتون:` استفاده کند. **این تنها جایی در زبان است که کد لاتین کامل (دستورات پایتون) مجاز است؛ بیرون از آن فقط شناسه‌های لاتین مجازند.**

```python
پایتون:
    import numpy as np
    arr = np.array([1, 2, 3])
    print(arr.mean())

نتایج_پایتون = پایتون_بازخورده()
```

### ۹.۳ تابع داخلی `پایتون`

```kolang
حاصل = پایتون(«1 + 2 * 3»)
حاصل بنویس   /۷

نامپای = پایتون_ماژول(«numpy»)
آرایه = آرایهِ([۱ و ۲ و ۳])نامپای
```

## ۱۰. استثناها و مدیریت خطا (به‌سبک سنتی)

علاوه بر مدل «خطا به‌عنوان مقدار» (بخش ۱۱)، کلنگ سیستم استثنای پایتون را نیز نگه می‌دارد. ساختار با افعال طبیعی (فعل‌پایانی برای `بگیر`):

```kolang
بپا:
    /کد ممکن است خطا دهد
خطای‌نوع بگیر بانام err:
    /مدیریت
خطای‌دیگر بگیر:
    /مدیریت
درنهایت:
    /همیشه اجرا می‌شود
```

پرتاب:

```kolang
خطای‌مقدار(«پیام») بده
```

تعریف استثنای سفارشی:

```kolang
گونه خطای‌سفارشی وارث استثنا:
    ساخت(خود و پیام):
        ساختِ(خود و پیام)والد()
```

## ۱۱. همزمانی و خطا به‌عنوان مقدار Go

### ۱۱.۱ مقدارهای برگشتی چندتایی و نوع خطا

```kolang
تعریف بگیر‌پیکربندی(مسیر) -> (گنجه و خطا):
    اگر فایل_وجود_دارد(مسیر) == غلط نباشد:
        تهی و خطا(«فایل پیدا نشد: » + مسیر) برگردان
    داده = بخوانِبازکردن(مسیر)()
    بارگذاریِ(داده)جیسون و تهی برگردان

cfg و err = بگیر‌پیکربندی(«config.json»)
اگر err == تهی نباشد:
    پیامِerr بنویس
    اتمام
```

> این الگو (به‌سبک Go) اکثر کاربردهای `بپا/بگیر` را جایگزین می‌کند. خطا یک مقدار است که بررسی می‌شود، نه چیزی که پرتاب می‌شود.

### ۱۱.۲ تارک‌ها (goroutines)

```kolang
تعریف کار(نام):
    برای ای از ۰ تا ۳:
        نام + «: » + متن(ای) بنویس
        خوابِزمان(۱)

برو کار(«الف»)
برو کار(«ب»)
برو کار(«ج»)
/هر سه به‌طور همزمان اجرا می‌شوند
```

### ۱۱.۳ کانال‌ها (channels)

```kolang
تعریف تولیدکننده(ch):
    برای ای از ۰ تا ۵:
        ch << ای       /ارسال به کانال
    ch ببند           /بستن کانال

تعریف مصرف‌کننده(ch):
    تاوقتی درست باشد:
        مقدار = >>ch  /دریافت از کانال
        اگر بسته‌استِch == درست باشد:
            اتمام
        مقدار بنویس

ch = کانال(صحیح و ۲)   /کانال صحیح با بافر ۲
برو تولیدکننده(ch)
برو مصرف‌کننده(ch)
```

### ۱۱.۴ تأخیر (defer)

```kolang
تعریف پردازش(مسیر):
    ف = بازکردن(مسیر و «r»)
    ببندِ()ف تأخیری
    /اگر خطا هم رخ دهد، ببندِ ف() حتماً اجرا می‌شود
    داده = بخوانِ()ف
    داده برگردان
```

### ۱۱.۵ رابط‌ها (interfaces)

رابط‌ها در بخش ۵.۹ توضیح داده شدند. نوع‌دهی ساختاری Go به این معناست که گونه‌ای که متدهای لازم را دارد، خودبه‌خود رابط را پیاده‌سازی می‌کند — بدون اعلان صریح.

## ۱۲. ماژول‌بندی و پروژه

هر فایل `.kolang` یک ماژول است. برای گرفتن از فایل دیگر:

```kolang
/فرض کنید فایل ابزار.kolang در همان پوشه است
از ابزار تابع‌کمکی بیار
```

ساختار پیشنهادی پروژه:

```text
پروژه/
├── main.kolang
├── ابزار.kolang
├── ماژول‌ها/
│   └── ریاضی_خودم.kolang
└── kolang.toml
```

فایل `kolang.toml`:

```text
[پروژه]
نام = "برنامه‌نمونه"
نسخه = "۱٫۰٫۰"

[واردکردن]
مسیرهای_اضافه = ["ماژول‌ها"]
```

## ۱۳. قالب‌بندی و سبک

- **تورفتگی:** ۴ فاصله. تب به ۴ فاصله تبدیل می‌شود.
- **حداکثر طول خط:** ۱۰۰ نویسه.
- **نام توابع و متغیرها:** `snake_case` فارسی: `تابع‌کمکی`.
- **نام گونه‌ها و رابط‌ها:** CamelCase فارسی: `حیوان`، `حساب‌کاربر`.
- **نام ثابت‌ها:** ثابت‌ها متغیرهای عادی هستند که مقدارشان تغییر نمی‌کند؛ قرارداد نام‌گذاری: نام معنادار انتخاب کنید (مثل `بیشترین_مقدار`).
- **افعال امری** برای دستورهای I/O و تغییر حالت به‌شکل فعل‌پایانی (بدون «را») استفاده می‌شوند.
- **دسترسی به اعضا** با اضافه‌ی kasra (`ِ`)، نه نقطه.
- **جداکننده‌ی آرگومان‌ها** «و» است، نه ویرگول. عملگر منطقی «and» کلمه‌ی `همچنین` است (بیرون پرانتز).
- **اولویت خطا به‌عنوان مقدار** بر استثنا Go.

## ۱۴. مثال‌های کامل

### ۱۴.۱ محاسبه فیبوناچی

```kolang
تعریف فیبوناچی(ن):
    اگر ن <= ۱ باشد:
        ن برگردان
    فیبوناچی(ن - ۱) + فیبوناچی(ن - ۲) برگردان

برای ای از ۰ تا ۱۰:
    فیبوناچی(ای) بنویس
```

### ۱۴.۲ گونه‌ی مدیریت کتاب

```kolang
گونه کتاب:
    ساخت(خود و عنوان و نویسنده):
        عنوانِخود = عنوان
        نویسندهِخود = نویسنده
        امانتِخود = غلط

    تعریف امانت‌دادن(خود):
        اگر امانتِخود == درست باشد:
            «این کتاب امانت است» بنویس
        وگرنه:
            امانتِخود = درست
            «کتاب امانت داده شد» بنویس

    تعریف بازگرداندن(خود):
        امانتِخود = غلط
        «کتاب بازگردانده شد» بنویس

گونه کتابخانه:
    ساخت(خود):
        کتاب‌های خود = []

    تعریف افزودن(خود و کتاب):
        کتاب  به کتاب‌های خود بیافزا

    تعریف فهرست‌نمایش(خود):
        برای کتاب در کتاب‌های خود:
            عنوانِکتاب + « - » + نویسندهِکتاب بنویس

کت = کتاب(«شاهنامه» و «فردوسی»)
امانت‌دادنِ()کت
بازگرداندنِ()کت
```

### ۱۴.۳ خواندن و پردازش فایل با تأخیر Go

```kolang
تعریف شمارش‌خطوط(مسیر) -> (صحیح و خطا):
    اگر فایل_وجود_دارد(مسیر) == غلط نباشد:
        ۰ و خطا(«فایل پیدا نشد») برگردان
    ف = بازکردن(مسیر و «r»)
    ببندِ()ف تأخیری
    خطوط = بخوانِ()ف
    طول(خطوط) و تهی برگردان

تعداد و err = شمارش‌خطوط(«داده.txt»)
اگر err == تهی نباشد:
    پیامِerr بنویس
    اتمام
«تعداد خطوط: » + متن(تعداد) بنویس
```

### ۱۴.۴ جنریتور و درک فهرست

```kolang
تعریف اعداد‌اول(حد):
    برای ن از ۲ تا حد:
        اول = درست
        برای ای از ۲ تا ن:
            اگر ن % ای == ۰ باشد:
                اول = غلط
                اتمام
        اگر اول == درست باشد:
            ن بساز

مجموع‌اول‌ها = جمع([ن برای ن در اعداد‌اول(۱۰۰)])
مجموع‌اول‌ها بنویس
```

### ۱۴.۵ کار با جیسون

```kolang
جیسون بیار

داده = {
    «نام»: «علی» و
    «سن»: ۲۵ و
    «مهارت‌ها»: [«پایتون» و «کلنگ»]
}

متن‌جیسون = دورریزِجیسون(داده و تورفتگی=۲)
متن‌جیسون بنویس

بازخورده = بارگذاریِجیسون(متن‌جیسون)
بازخورده[«نام»] بنویس
```

### ۱۴.۶ کار با تاریخ

```kolang
از تقویم تاریخ بیار

امروز = امروزِتاریخ()
«امروز: » + متن(امروز) بنویس

تولد = تاریخ(۱۳۷۰ و ۵ و ۱۵)
سن = سالِامروز - سالِتولد
«سن: » + متن(سن) بنویس
```

### ۱۴.۷ مدیریت خطا با استثنا

```kolang
تعریف تقسیم‌امن(الف و ب):
    بپا:
        الف ÷ ب برگردان
    خطای‌صفر بگیر:
        «خطا: تقسیم بر صفر» بنویس
        تهی برگردان
    خطای‌نوع بگیر بانام err:
        «نوع نادرست: » + متن(err) بنویس
        err بده

نتیجه = تقسیم‌امن(۱۰ و ۰)
نتیجه بنویس
```

### ۱۴.۸ ماشین‌حساب

```kolang
تعریف ماشین‌حساب():
    «ماشین حساب کلنگ» بنویس
    «برای خروج 'خروج' را بنویسید» بنویس

    تاوقتی درست باشد:
        عبارت = «>> » بگیر
        اگر عبارت == «خروج» باشد:
            اتمام
        بپا:
            نتیجه = اجرا(عبارت)
            نتیجه بنویس
        استثنا بگیر بانام err:
            «خطا: » + متن(err) بنویس

ماشین‌حساب()
```

### ۱۴.۹ همزمانی با تارک و کانال Go

```kolang
تعریف کارگر(شناسه و ch):
    برای ای از ۰ تا ۳:
        ch << («کارگر » + متن(شناسه) + «: » + متن(ای))
    ch ببند

ch = کانال(متن و ۵)

برو کارگر(۱ و ch)
برو کارگر(۲ و ch)
برو کارگر(۳ و ch)

/جمع‌آوری نتایج تا زمانی که همه‌ی کارگرها کانال را ببندند
تعداد_ببند = ۰
تاوقتی تعداد_ببند < ۳ باشد:
    مقدار = >>ch
    اگر بسته‌استِch == درست باشد:
        تعداد_ببند += ۱
        بروبعدی
    مقدار بنویس
```

### ۱۴.۱۰ رابط و چندریختی Go

```kolang
رابط شکل:
    تعریف مساحت(خود) -> اعشاری
    تعریف محیط(خود) -> اعشاری

گونه مستطیل:
    ساخت(خود و عرض و ارتفاع):
        عرضِخود = عرض
        ارتفاعِخود = ارتفاع

    تعریف مساحت(خود) -> اعشاری:
        عرضِخود × ارتفاعِخود برگردان

    تعریف محیط(خود) -> اعشاری:
        ۲ × (عرضِخود + ارتفاعِخود) برگردان

گونه دایره:
    ساخت(خود و شعاع):
        شعاعِخود = شعاع

    تعریف مساحت(خود) -> اعشاری:
        پیِریاضی × شعاعِخود * ۲ برگردان

    تعریف محیط(خود) -> اعشاری:
        ۲ × پیِریاضی × شعاعِخود برگردان

تعریف معرفی(ش: شکل):
    «مساحت: » + متن(مساحتِ()ش) بنویس
    «محیط: » + متن(محیطِ()ش) بنویس

اشکال = [مستطیل(۳ و ۴) و دایره(۵)]
برای ش در اشکال:
    معرفی(ش)
```

## ۱۵. معماری پیاده‌سازی

کلنگ به‌سبک CPython پیاده‌سازی می‌شود.

### ۱۵.۱ مراحل اجرا

1. **لِکسر (Lexer):** متن کلنگ را به توکن تبدیل می‌کند. توکن‌ها شامل شناسه‌ی فارسی، عدد، رشته (گیومه)، عملگر (از جمله اضافه‌ی `ِ`)، کلمه‌ی کلیدی، تورفتگی هستند. ارقام فارسی به لاتین نرمال می‌شوند و نیم‌فاصله به‌عنوان بخشی از شناسه پذیرفته می‌شود. **اضافه‌ی kasra (`ِ`) به‌عنوان یک عملگر میان‌گذاری تایم‌بندی می‌شود** (مانند `+` یا `-`)، نه بخشی از شناسه.
1. **پارسر (Parser):** توکن‌ها را به AST تبدیل می‌کند. گرامر LL(1) با پشتیبانی از تورفتگی به‌سبک پایتون. ساختارهای فعل‌پایانی (بدون «را») (مثل `x بنویس`، `ریاضی بیار`، `خطای‌صفر بگیر:`)، شرط‌های التزامی (`اگر ... باشد:`)، دسترسی به اعضا با اضافه (`جذرِ ریاضی`)، انتساب چندتایی، `برو`، `تأخیر`، و `<<` / `>>` کانال در سطح گرامر پشتیبانی می‌شوند. **«و» در بافت پرانتز جداکننده‌ی آرگومان است؛ عملگر منطقی «and» بیرون پرانتز کلمه‌ی «همچنین» است.**
1. **ارزیاب درختی (Eval):** در v1، AST مستقیماً اجرا می‌شود (tree-walking) — `internal/eval`.
1. **کامپایلر و ماشین مجازی:** در v0.7+ اضافه می‌شوند. کامپایلر AST را به بایت‌کد تبدیل می‌کند و ماشین مجازی (پشته‌محور، با فریم‌های پشته برای توابع و زمان‌بند سبک برای تارک‌ها) آن را اجرا می‌کند.

### ۱۵.۲ ساختار بایت‌کد

بایت‌کد مشابه CPython است، به‌علاوه‌ی چند opcode الهام‌گرفته از Go:

- `LOAD_CONST`، `LOAD_NAME`، `STORE_NAME`
- `LOAD_ATTR` (دسترسی با اضافه) — معادل `جذرِ ریاضی`
- `BINARY_ADD`، `BINARY_SUBTRACT`، …
- `COMPARE_OP`
- `POP_JUMP_IF_FALSE`
- `CALL_FUNCTION`، `MAKE_FUNCTION`، `RETURN_VALUE`
- `BUILD_LIST`، `BUILD_MAP`، `BUILD_TUPLE_UNPACK`
- `GET_ITER`، `FOR_ITER`
- `SETUP_EXCEPT`، `SETUP_FINALLY`
- `IMPORT_NAME`، `IMPORT_FROM`
- `DEFER_CALL` Go
- `GO_SPAWN` Go
- `CHAN_SEND`، `CHAN_RECV`، `CHAN_CLOSE` Go

### ۱۵.۳ همکاری با پایتون

- **نگاشت ایستا:** نام فارسی → نام پایتون (جدول در بخش ۸).
- **نگاشت پویا:** از طریق `Python.h` یا `cgo` به پایتون متصل می‌شود.

> در v1، کلنگ یک ارزیاب درختی (tree-walking) در Go است؛ ماژول‌های کتابخانه‌ی استاندارد (مانند `ریاضی`) به‌صورت بومی در Go پیاده‌سازی شده‌اند. نگاشت پویا به پایتون (از طریق `cgo`/`Python.h`) و کامپایل AST به بایت‌کد پایتون (`.pyc`) برای نسخه‌های بعدی (v0.7+) برنامه‌ریزی شده‌اند. ویژگی‌های Go (تارک، کانال) به‌طور شبیه‌سازی‌شده پیاده‌سازی شده‌اند و در فاز ۲ به VM مستقل منتقل می‌شوند.

### ۱۵.۴ ساختار مخزن (v1)

```text
kolang/
├── SPEC.md
├── README.md
├── go.mod
├── cmd/
│   └── kolang/
│       └── main.go          # CLI (اجرای فایل، -c، REPL)
├── internal/
│   ├── token/
│   │   └── token.go         # انواع توکن و کلمات کلیدی
│   ├── lexer/
│   │   └── lexer.go         # لِکسر (ارقام فارسی، رشته‌های «»، توضیحات / و //، اضافه)
│   ├── ast/
│   │   └── ast.go           # انواع گره‌های AST
│   ├── parser/
│   │   └── parser.go        # پارسر بازگشتی-نزولی (recursive-descent)
│   └── eval/
│       ├── eval.go          # ارزیاب درختی (tree-walking) — هسته
│       ├── eval_expr.go
│       ├── eval_members.go
│       ├── eval_control.go
│       ├── eval_oop.go
│       ├── eval_exceptions.go
│       ├── eval_generators.go
│       ├── eval_decorators.go
│       ├── eval_concurrency.go
│       ├── eval_comp.go
│       ├── eval_strings.go
│       ├── eval_types.go
│       ├── eval_helpers.go
│       ├── eval_assign.go
│       ├── builtins.go
│       └── modules.go
└── examples/
    ├── hello.kolang
    ├── fibonacci.kolang
    ├── channel.kolang
    └── interfaces.kolang
```

پوشه‌های `compiler/`، `bytecode/` و `vm/` (کامپایلر، بایت‌کد و ماشین مجازی) برای v0.7+ برنامه‌ریزی شده‌اند و فعلاً به تعویق افتاده‌اند؛ در v1 اجرا مستقیم AST با ارزیاب درختی است.

### ۱۵.۵ CLI

```text
kolang script.kolang          # اجرای فایل
kolang repl                   # محیط تعاملی
kolang -c "«سلام» بنویس"   # اجرای خط فرمان
```

`kolang compile`، `kolang fmt` و `kolang vet` در برنامه‌ی راه برای نسخه‌های بعدی هستند (در v1 هنوز پیاده‌سازی نشده‌اند).

## ۱۶. نقشه راه (Roadmap)

توسعه‌ی کلنگ در **دو فاز** برنامه‌ریزی شده است:

> **فاز ۱ (نسخه‌های ۰٫x تا ۱٫x) — پایتون فارسی:** زبانی پویا با GC که به بایت‌کد پایتون کامپایل و روی CPython اجرا می‌شود. هدف: دسترسی کامل به اکوسیستم پایتون با سینتکس فارسی طبیعی. نوع‌دهی تدریجی (اختیاری، زمان-اجرا). همزمانی شبیه‌سازی‌شده.
>
> **فاز ۲ (نسخه‌های ۲٫x+) — زبان سیستمی فارسی (هدف Rust-like):** VM مستقل با سیستم نوع استاتیک، ownership/borrowing، traitها، نوع‌های جمع (sum types)، تطبیق الگو (pattern matching)، و genericها. حاشیه‌نویسی‌های فاز ۱ به بررسی زمان-کامپایل تبدیل می‌شوند.

### ۱۶.۱ فاز ۱ — پایتون فارسی

- **نسخه‌ی ۰٫۱ — پایه:**
  - لِکسر کامل فارسی (شناسه، عدد، رشته با گیومه، عملگر، تورفتگی، نیم‌فاصله، اضافه)
  - پارسر: انتساب، شرط (با `باشد`)، حلقه، تابع، افعال امری، دسترسی با اضافه
  - ارزیاب درختی (tree-walking) به‌عنوان پایه‌ی اولیه
  - توابع داخلی اصلی: `بنویس`، `بگیر`، `طول`، `بازه`
  - آوردن ماژول (`ریاضی بیار` / `از ریاضی جذر بیار`)
- **نسخه‌ی ۰٫۲ — شیءگرایی و رابط‌ها:**
  - گونه، وارث، متد، `خود`، `والد`
  - رابط‌ها و نوع‌دهی ساختاری Go
- **نسخه‌ی ۰٫۳ — خطا و استثنا:**
  - مقدار برگشتی چندتایی و نوع `خطا` Go
  - `بپا`/`خطا بگیر`/`درنهایت`، `بده`
  - `تأخیری` Go
- **نسخه‌ی ۰٫۴ — جنریتور و پوشش:**
  - `بساز`، `پوشش`
- **نسخه‌ی ۰٫۵ — نوع‌دهی تدریجی:**
  - حاشیه‌نویسی نوع متغیر و پارامتر (اختیاری، زمان-اجرا)
  - بررسی نوع در زمان اجرا
- **نسخه‌ی ۰٫۶ — همزمانی (شبیه‌سازی‌شده):**
  - تارک (`برو`) و کانال‌ها Go
  - پیاده‌سازی روی asyncio یا threadpool (به‌دلیل GIL پایتون)
- **نسخه‌ی ۰٫۷ — بایت‌کد و کامپایل به .pyc:**
  - کامپایل AST به بایت‌کد پایتون و اجرای مستقل
- **نسخه‌ی ۰٫۸ — همکاری پایتون:**
  - نگاشت ماژول‌ها، بلوک `پایتون:`
- **نسخه‌ی ۱٫۰ — انتشار فاز ۱:**
  - کتابخانه‌ی استاندارد فارسی کامل
  - REPL، `kolang fmt`، `kolang vet`
  - مستندات کامل

### ۱۶.۲ فاز ۲ — زبان سیستمی فارسی (هدف Rust-like)

پس از تثبیت فاز ۱، توسعه‌ی فاز ۲ با هدف تبدیل کلنگ به زبانی سیستمی و ایمن (مانند Rust) آغاز می‌شود. ویژگی‌های برنامه‌ریزی‌شده:

- **VM مستقل:** ماشین مجازی بومی کلنگ (نه CPython). بایت‌کد و فرمت اجرایی مستقل.
- **سیستم نوع استاتیک:** حاشیه‌نویسی‌های فاز ۱ به بررسی زمان-کامپایل تبدیل می‌شوند. نوع‌دهی استاتیکی کامل (اختیاری در فاز ۱، اجباری در فاز ۲).
- **Genericها:** نوع‌های پارامتریک. `گونه پشته<ت>:` — پشته‌ی نوع `ت`.
- **Traitها:** مجموعه‌ی متد با dispatch استاتیک (برخلاف رابط‌های فاز ۱ که runtime هستند). `صفت نمایش { ... }` + `برای نوع ت رهی نمایش { ... }`.
- **نوع‌های جمع (sum types / enums):** `گونه نتیجه<ت> = موفق(ت) یا شکست(خطا)` — با استفاده از `یا`.
- **تطبیق الگو (pattern matching):** `پوشش مقدار { موفق(م) => ... شکست(ه) => ... }`.
- **Ownership و borrowing:** مدیریت حافظه‌ی ایمن بدون GC (مانند Rust). `&` برای قرض‌گیری، قوانین عمر (lifetime).
- **عملگر پخش خطا:** `?` برای بازگشت زودهنگام هنگام خطا (مانند Rust). `مقدار = کار()؟`.
- **دیدگاه ماژول:** `عمومی` / `خصوصی` برای کنترل صادرات.
- **عملگرهای بیتی:** `&` `|` `^` `~` `<<` `>>`.

> توجه: فاز ۲ نیازمند طراحی عمیق سیستم نوع و ownership است و زمان‌بر خواهد بود. فاز ۱ به‌تنهایی یک زبان کامل و مفید است.

## ۱۷. ملاحظات طراحی

### ۱۷.۱ راست‌به‌چپ (RTL)

کد منبع از راست به چپ نمایش داده می‌شود. تورفتگی در سمت راست قرار می‌گیرد. نمادهای `{}`، `[]`، `()` همان‌طور که هست ذخیره می‌شوند؛ در نمایش RTL جهت آن‌ها به‌صورت خودکار معکوس دیده می‌شود.

### ۱۷.۲ نیم‌فاصله در شناسه‌ها

شناسه‌های چندبخشی فارسی (مثل `برگردان`، `امانت‌دادن`) ممکن است شامل نیم‌فاصله باشند. لِکسر باید نیم‌فاصله را به‌عنوان بخشی از شناسه بپذیرد.

### ۱۷.۳ ارقام فارسی

ارقام فارسی `۰-۹` در سطح لِکسر به ارقام لاتین `0-9` تبدیل می‌شوند.

### ۱۷.۴ رشته‌ها با گیومه‌ی «»

گیومه‌های `« »` در متن RTL پایدارند (BIDI-stable) و به همین دلیل به‌عنوان جداکننده‌ی رشته انتخاب شدند؛ بک‌تیک در مرحله‌ی طراحی بررسی شد اما رد شد. متن‌های «» به‌صورت پیش‌فرض f-string هستند (درون‌یابی با `{...}`) و چندخطی را به‌طور طبیعی پشتیبانی می‌کنند. برای نمایش لفظی آکولاد از `{{` و `}}` استفاده کنید.

### ۱۷.۵ اضافه به‌جای نقطه

دسترسی به اعضا با اضافه‌ی kasra (`ِ` U+0650) انجام می‌شود. این تصمیم کلنگ را شبیه نثر فارسی می‌کند: `جذرِ ریاضی` به‌جای `ریاضی.جذر`. اضافه از راست به چپ می‌چسبد: `روشِ والدِ خود` = `self.parent.method`. در لِکسر، اضافه به‌عنوان یک عملگر میان‌گذاری شناخته می‌شود، نه بخشی از شناسه.

### ۱۷.۶ شرط‌های نمادین

کلنگ فقط شکل نمادین را برای مقایسه می‌پذیرد: `اگر x == ۵ باشد:`. شکل طبیعی فارسی (`اگر x برابر ۵ باشد:`) حذف شده است. فعل التزامی `باشد`/`نباشد` پس از مقایسه‌ی نمادین می‌آید. بنابراین هر شرط یک مقایسه‌ی نمادین صریح به‌همراه `باشد` یا `نباشد` است.

### ۱۷.۷ بدون درستی ضمنی (No implicit truthiness)

برخلاف پایتون، کلنگ **تبدیل ضمنی به بولی انجام نمی‌دهد**. یک شرط باید همیشه یک مقایسه‌ی صریح داشته باشد. این یعنی:

- `اگر x:` — **نامعتبر** (حتی اگر `x` بولی باشد؛ باید `اگر x == درست باشد:` نوشته شود).
- `اگر فهرست:` — **نامعتبر**؛ باید `اگر طول(فهرست) == ۰ نباشد:`.
- `اگر x == ۰ باشد:` — **معتبر** (مقایسه‌ی صریح).
- `تاوقتی درست باشد:` — **معتبر** (`درست` یک لیترال بولی صریح است، نه یک تبدیل ضمنی؛ این شکل استاندارد حلقه‌ی بی‌پایان است).

این قانون از باگ‌های خاموش پایتون جلوگیری می‌کند (مثلاً `if x:` وقتی `x = 0` یا `x = []` یا `x = None`) و صراحت Go را به‌جا می‌گذارد. در پارسر، شرطی که یک عبارت لخت (نه یک مقایسه) باشد به‌عنوان خطای نحوی گزارش می‌شود — به‌جز لیترال بولی `درست` / `غلط`. **فعل `باشد`/`نباشد` پس از مقایسه الزامی است.**

### ۱۷.۸ «و» و «همچنین»

در کلنگ، «و» دو کاربرد ندارد (برخلاف v5). «و» فقط جداکننده‌ی آرگومان است (درون پرانتز، مثل `جمع(۱ و ۲ و ۳)`) و در انتساب چندتایی سمت چپ (مثل `نتیجه و خطا = ...`). عملگر منطقی «and» کلمه‌ی جداگانه‌ی `همچنین` است (بیرون پرانتز، مثل `درست همچنین غلط`). این جداسازی از ابهام جلوگیری می‌کند: «و» همیشه «and» منطقی نیست، بلکه بستگی به بافت دارد. نفی منطقی `نباشد` است.

### ۱۷.۹ افعال امری

دستورهای I/O و تغییر حالت به‌شکل **فعل‌پایانی (بدون «را»)** نوشته می‌شوند: `x بنویس`، `نام بگیر`، `x برگردان`. این شامل `بنویس`، `بگیر`، `برگردان`، `بیافزا`، `حذف‌کن`، `بده`، `بیار` (import)، `ببند` می‌شود. استثناها: `اتمام` و `بروبعدی` که بدون مفعول‌اند.

### ۱۷.۱۰ چرا «خطا به‌عنوان مقدار» به‌جای استثنا؟ Go

الهام‌گرفته از Go: خطا به‌عنوان مقدار، کنترل جریان را صریح و قابل‌ردیابی می‌کند. برنامه‌نویس مجبور است خطا را بررسی کند. این استواری برنامه‌ها را در برابر خطاهای خاموش بیافزایش می‌دهد. استثنا (`بپا/بگیر`) برای موارد واقعاً غیرمنتظره حفظ می‌شود.

### ۱۷.۱۱ چرا تارک و کانال؟ Go

مدل CSP گو (ارتباط با کانال به‌جای حافظه‌ی مشترک) ساده‌تر و امن‌تر از نخ‌ها و قفل‌ها است. کلنگ آن را وام می‌گیرد تا همزمانی برای برنامه‌نویس فارسی‌زبان ساده و در دسترس باشد.

## ۱۸. جمع‌بندی

کلنگ زبانی است که:

- تمام قابلیت‌های پایتون را دارد.
- کلمات کلیدی، عملگرها و افعال آن فارسی هستند؛ شناسه‌ها می‌توانند فارسی یا انگلیسی باشند (فقط در بلوک `پایتون:` کد لاتین کامل مجاز است).
- دستورهای I/O و تغییر حالت به‌شکل **فعل‌پایانی** نوشته می‌شوند (`«سلام» بنویس`).
- شرط‌ها با مقایسه‌ی نمادین صریح نوشته می‌شوند (`اگر سن == ۱۸ باشد:`) و **درستی ضمنی وجود ندارد**.
- رشته‌ها با **گیومه‌های `« »`** نوشته می‌شوند.
- دسترسی به اعضا با **اضافه‌ی kasra** (`ِ`) انجام می‌شود، نه نقطه (`جذرِ ریاضی`).
- آرگومان‌ها با **«و»** جدا می‌شوند، نه ویرگول (`جمع(۱ و ۲ و ۳)`).
- گونه‌ها (class) و رابط‌ها (interface) با `گونه` و `رابط` تعریف می‌شوند.
- گرفتن ماژول با فعل **«بیار»** در پایان جمله است (`ریاضی بیار`، `از ریاضی جذر بانام ریشه بیار`).
- استثناها با افعال **«بپا/خطا بگیر/درنهایت/بده»** طبیعی‌اند.
- از Go الهام گرفته برای استواری: **مقدار برگشتی چندتایی**، **نوع خطا**، **تارک**، **کانال**، **تأخیر**، **رابط ساختاری**.
- به کتابخانه‌ی استاندارد پایتون با نام فارسی دسترسی دارد.
- با خط لوله‌ی لِکسر ← پارسر ← AST ← ارزیاب درختی (tree-walking) طراحی شده است (کامپایلر و ماشین مجازی در نسخه‌های بعدی).
- در v1 به‌صورت ارزیاب درختی در Go اجرا می‌شود و به کتابخانه‌ی استاندارد پایتون با نام فارسی دسترسی دارد.

> این سند مرجع زبانی است و با پیشرفت پیاده‌سازی به‌روزرسانی می‌شود.
